# Changing a NEXT_PUBLIC_* var requires a new production deploy before it applies.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
# Required only when passing secretKey into clerkMiddleware (dynamic keys).
CLERK_ENCRYPTION_KEY=
