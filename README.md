# Hypher

> Cursor already has the code. Hypher has the decisions that never made it into the code. The next agent starts warm.

**Read [docs/PRODUCT.md](docs/PRODUCT.md) before changing the product.** That is the only product document.
**Build order:** [docs/PLAN.md](docs/PLAN.md).

Dump messy project context. Hypher compiles one Builder Brief. Agents read it, work in the IDE, and write back. The next session starts warm.
