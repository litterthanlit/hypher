# Agents

Read [docs/PRODUCT.md](docs/PRODUCT.md) first. It is the only product source of truth.

Hypher is project memory under agents: dump → one brief → writeback. Cursor already has the code.

Do not rebuild the canvas, daily digest, health rings, ambient ask, Notion import, or extra Pulse panels. Those were cut.

How, not what:

- Cursor plugin: `extensions/cursor/README.md`
- Handoff CLI: `hypher-web/docs/agent-handoff.md`
- UI styling: `hypher-web/STYLING.md`
